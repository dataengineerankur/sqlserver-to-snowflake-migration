version: 2

sources:
  - name: raw
    database: "{{ env_var('SNOWFLAKE_DATABASE', 'ANALYTICS') }}"
    schema: RAW
    tables:
      - name: customers
      - name: products
      - name: orders
      - name: order_items
